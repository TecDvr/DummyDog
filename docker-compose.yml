version: "3"
services:
    reactdog:
        image: zachgw/dummydog:latest
        container_name: reactdog
        ports:
            - "3000:3000"
        restart: on-failure
        tty: true
        stdin_open: true
